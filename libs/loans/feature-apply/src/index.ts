import { Route } from '@angular/router';
import { LoanApplyComponent } from './lib/loan-apply.component';

export const LOANS_APPLY_ROUTES: Route[] = [
  { path: '', component: LoanApplyComponent },
];
